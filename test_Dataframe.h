/*
    afficher_noms_colonnes(mydf);
    afficher_ligne(mydf,1);
    afficher_colonne(mydf,1);

    remplacer_valeur(mydf);
    afficher_dataframe(mydf);

    renommer_colonne(mydf);
    afficher_dataframe(mydf);

    ajouter_ligne(mydf);
    afficher_dataframe(mydf);

    supprimer_ligne(mydf);
    afficher_dataframe(mydf);

    supprimer_colonne(mydf);
    afficher_dataframe(mydf);

    ajouter_colonne(mydf);
    afficher_dataframe(mydf);

    verif_valeur(mydf);
    */
